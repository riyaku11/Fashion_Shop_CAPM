_schema-version: 3.3.0
ID: Fashion_shop
description: A simple CAP project.
version: 1.0.0
modules:
- name: Fashion_shop-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Fashion_shop-auth
  - name: Fashion_shop-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: Fashion_shop-db-deployer
  type: hdb
  path: db
  requires:
  - name: Fashion_shop-db
  parameters:
    buildpack: nodejs_buildpack
- name: Fashion_shop
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: Fashion_shop-auth
  - name: Fashion_shop-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  - name: Fashion_shop-repo-host
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: Fashion_shop-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Fashion_shop-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comfashionshopfashionitemsui.zip
      name: comfashionshopfashionitemsui
      target-path: resources/
- name: comfashionshopfashionitemsui
  type: html5
  path: app/fashion-items-ui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Fashion_shop-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: Fashion_shop-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: Fashion_shop-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Fashion_shop-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Fashion_shop-html5-service
    service-plan: app-host
- name: Fashion_shop-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Fashion_shop-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install --legacy-peer-deps 
    - npx cds build --production
